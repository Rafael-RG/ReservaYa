namespace ReservaYa.Api.Models;

public enum ServiceType
{
    APPOINTMENT, // Standard (Haircut, Doctor)
    EVENT,       // Classes, Workshops (Yoga, Crossfit)
    ON_SITE,     // Home services (Cleaning, Private Chef)
    TABLE        // Gastronomy (Restaurants)
}
